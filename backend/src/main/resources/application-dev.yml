spring:
  data:
    mongodb:
      uri: ${SPRING_DATA_MONGODB_URI:mongodb://localhost:27017/ai_project_architect_dev}

app:
  jwt:
    secret: ${JWT_SECRET:dev-only-secret-please-change-me-1234567890-abcdefgh}

  gemini:
    api-key: ${APP_GEMINI_API_KEY:your_gemini_api_key_here}

logging:
  level:
    com.projectarchitect: DEBUG